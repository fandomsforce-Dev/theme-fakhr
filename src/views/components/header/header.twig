{#
| Variable         | Type    | Description                     |
|------------------|---------|---------------------------------|
| cart             | object  |                                 |
| cart.items_count | int      |                                 |
| cart.total       | string  | Formatted total ex: "١٠٠ ر.س"   |
#}

{% set important_links = theme.settings.get('important_links') %}
{% set logo_position = theme.settings.get('logo_position', 'center') %}
{% set menu_position = theme.settings.get('menu_position', 'right') %}

{# حاوية ذكية تحمل إعدادات لوحة التحكم كـ Classes ديناميكية للاعتماد عليها بالكامل في الـ CSS #}
<div class="theme-header-setting logo-{{ logo_position }} menu-{{ menu_position }}">

<header class="store-header">
  {# Top Nav #}
  <div class="top-navbar">
    <div class="container flex justify-between">
      <div class="flex-1 flex items-center gap-2">
          {% if important_links %}
            <salla-menu source="footer" topnav></salla-menu>
          {% endif %}

          <div class="header-buttons">
              {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
                  <salla-localization-modal show-trigger></salla-localization-modal>
              {% endif %}
          </div>

          {% if store.scope %}
              <button class="btn--rounded-gray"
                      onclick="salla.event.dispatch('scopes::open', {'mode': 'default'})">
                  <i class="sicon-location rtl:ml-1 ltr:mr-1"></i> {{ store.scope.name }}
              </button>
          {% endif %}

          {# 🎯 التطوير الهندسي الجديد لمربع البحث مع الطبقة النصية المتحركة الصاعدة #}
          <div class="header-search flex-1 min-w-[240px] max-w-[450px] relative">
            <div class="fandom-search-box relative w-full">
              
              <salla-search inline oval height="36"></salla-search>
              
              {# تأثير النصوص الصاعدة المربوط بلوحة التحكم #}
              <div id="search-dynamic-overlay" class="fandom-search-overlay" data-search-words="{{ theme.settings.search_words }}">
                  <span class="static-txt">ابحث عن: </span>
                  <span id="changing-txt" class="changing-txt"></span>
              </div>

            </div>
          </div>
      </div>

      <salla-contacts is-header></salla-contacts>
    </div>
  </div>

  {# Main Nav #}
  <div id="mainnav" class="main-nav-container shadow-default bg-white flex items-center py-4">
      <div class="inner bg-inherit w-full">
          <div class="container">
              
              {# حاوية الهيدر الرئيسية المرنة #}
              <div class="flex w-full items-center justify-between relative" id="header-inner-flex">

                  {# 1. قسم اللوجو #}
                  <div class="logo-block flex items-center flex-none" id="logo-wrapper">
                      <a class="lg:hidden mburger mburger--collapse leading-none rtl:ml-4 ltr:mr-4" href="#mobile-menu" aria-label="Open-menu">
                        <i class="sicon-menu text-primary text-2xl"></i>
                      </a>

                      <a class="navbar-brand m-0" href="{{ store.url }}" style="max-width: var(--logo-width, auto) !important; display: block; width: 100%;">
                          <img fetchpriority="high" 
                               width="100%" 
                               height="100%" 
                               loading="eager" 
                               src="{{ store.logo|cdn(300) }}" 
                               alt="{{ store.name }} logo">
                          
                          {% if is_page('index') %}
                            <h1 class="sr-only">{{ store.name }}</h1>
                          {% else %}
                            <h2 class="sr-only">{{ store.name }}</h2>
                          {% endif %}
                      </a>
                  </div>

                  {# 2. قسم المنيو الرئيسي #}
                  <div class="menu-block flex" id="menu-wrapper">
                      <custom-main-menu></custom-main-menu>
                  </div>

                  {# 3. قسم السلة والحساب وتجربة الاتصال #}
                  <div class="actions-block flex items-center justify-end flex-none gap-2" id="actions-wrapper">
                      <div class="welcome-message text-green-600 font-bold mx-2 px-3 py-1 bg-green-50 rounded-full">
                        تست
                      </div>
                      <salla-user-menu avatar-only show-header show-trigger></salla-user-menu>
                      <salla-cart-summary class="ml-4 rtl:ml-[unset] rtl:mr-4">
                        <i slot="icon" class="header-btn__icon icon sicon-shopping-bag"></i>
                      </salla-cart-summary>
                  </div>

              </div>

          </div>
      </div>
  </div>
</header>

</div>

{# 🎯 1. الستايل العام النظيف والمستقر لجميع الحالات (متوافق 100% مع معايير محررات الأكواد) #}
<style>
  body .navbar-brand {
      float: none !important;
      margin: 0 !important;
      max-width: var(--logo-width, auto) !important;
      width: var(--logo-width, 100%) !important;
  }

  body .navbar-brand img {
      width: 100% !important;
      height: auto !important;
      max-width: 100% !important;
      object-fit: contain !important;
  }

  @media (min-width: 1024px) {
      body #header-inner-flex {
          display: flex !important;
          width: 100% !important;
          align-items: center !important;
          justify-content: space-between !important;
          position: relative !important;
      }

      body #menu-wrapper,
      body #menu-wrapper custom-main-menu,
      body #menu-wrapper ul {
          display: flex !important;
          flex-direction: row !important;
          align-items: center !important;
          gap: 16px !important;
          white-space: nowrap !important;
      }

      body #logo-wrapper, 
      body #actions-wrapper {
          z-index: 30 !important;
      }

      /* وضعية: لوجو يمين ومنيو يمين */
      .theme-header-setting.logo-right.menu-right #menu-wrapper {
          position: static !important;
          transform: none !important;
          flex: 1 !important;
          justify-content: flex-start !important;
          padding-right: 35px !important;
          padding-left: 0 !important;
          margin: 0 !important;
          width: auto !important;
      }

      /* وضعية: لوجو سنتر ومنيو سنتر */
      .theme-header-setting.logo-center.menu-center #header-inner-flex {
          flex-direction: column !important;
          gap: 16px !important;
          align-items: center !important;
          justify-content: center !important;
      }
      .theme-header-setting.logo-center.menu-center #logo-wrapper,
      .theme-header-setting.logo-center.menu-center #menu-wrapper {
          position: static !important;
          transform: none !important;
          margin: 0 auto !important;
          width: auto !important;
          justify-content: center !important;
      }
      .theme-header-setting.logo-center.menu-center #actions-wrapper {
          position: absolute !important;
          left: 0 !important;
          top: 50% !important;
          transform: translateY(-50%) !important;
      }

      /* وضعية: لوجو سنتر ومنيو يمين */
      .theme-header-setting.logo-center:not(.menu-center) #logo-wrapper {
          position: absolute !important;
          left: 50% !important;
          top: 50% !important;
          transform: translate(-50%, -50%) !important;
      }
      .theme-header-setting.logo-center:not(.menu-center) #menu-wrapper {
          position: static !important;
          transform: none !important;
          flex: 1 !important;
          justify-content: flex-start !important;
          padding-right: 0 !important;
      }
  }

  /* 🎯 التنسيق الهندسي لثبات حجم حقل البحث وتشغيل أنيميشن صعود النصوص */
  .fandom-search-box salla-search {
      width: 100% !important;
      display: block !important;
  }
  .fandom-search-overlay {
      position: absolute;
      right: 18px;
      top: 50%;
      transform: translateY(-50%);
      pointer-events: none; /* لتمرير الضغطة والكتابة للحقل الحقيقي بالخلف */
      font-size: 13px;
      color: #9c9c9c;
      display: flex;
      align-items: center;
      gap: 3px;
      z-index: 10;
      font-weight: 400;
      transition: opacity 0.25s ease;
  }
  .changing-txt {
      display: inline-block;
      color: #2a2a2a;
      font-weight: 500;
      transition: transform 0.35s cubic-bezier(0.645, 0.045, 0.355, 1), opacity 0.35s ease;
      opacity: 1;
      transform: translateY(0);
  }
  .changing-txt.slide-out {
      opacity: 0;
      transform: translateY(-14px); /* الهروب للأعلى */
  }
  .changing-txt.slide-in {
      opacity: 0;
      transform: translateY(14px); /* الدخول من الأسفل */
  }
</style>

{# 🎯 2. الحل الهندسي الصارم لقفل ارتفاع الشريط الأبيض من الانضغاط (لوجو يمين + منيو سنتر) #}
{% if logo_position == 'right' and menu_position == 'center' %}
<style>
  @media (min-width: 1024px) {
      body .main-nav-container,
      body #mainnav,
      body header [id^="mainnav"],
      body .is-sticky #mainnav,
      body .fixed-header #mainnav,
      body [class*="sticky"] [id^="mainnav"],
      body [class*="fixed"] [id^="mainnav"],
      body .is-sticky .main-nav-container,
      body .fixed-header .main-nav-container,
      body [id^="mainnav"] .inner {
          padding-top: 16px !important;
          padding-bottom: 16px !important;
          height: 84px !important;
          min-height: 84px !important;
          display: flex !important;
          align-items: center !important;
      }

      body #menu-wrapper,
      body .is-sticky #menu-wrapper,
      body .fixed-header #menu-wrapper {
          position: absolute !important;
          left: 50% !important;
          top: 50% !important;
          transform: translate(-50%, -50%) !important;
          margin: 0 !important;
          padding: 0 !important;
          width: max-content !important; 
          max-width: 60% !important; 
          display: flex !important;
          justify-content: center !important;
          z-index: 10 !important;
      }
  }
</style>
{% endif %}

{# 🎯 3. سكريبت التبديل العمودي الانسيابي والمراقبة التفاعلية لحقل البحث #}
<script>
  document.addEventListener("DOMContentLoaded", function() {
      const overlay = document.getElementById('search-dynamic-overlay');
      const changingTxt = document.getElementById('changing-txt');
      const boxWrapper = document.querySelector('.fandom-search-box');
      
      if (!overlay || !changingTxt || !boxWrapper) return;

      const rawData = overlay.getAttribute('data-search-words');
      let searchPhrases = rawData && rawData.trim() !== "" 
          ? rawData.split(',').map(word => word.trim()) 
          : ["يد تحكم GameSir", "سلك شاحن آيفون", "ستاند جوال مكتبي"];

      let currentIndex = 0;

      function triggerVerticalSlide() {
          // 1. بدء تأثير الهروب والصعود للأعلى للكلمة القديمة
          changingTxt.classList.add('slide-out');
          
          setTimeout(() => {
              // 2. تحديث الكلمة وتجهيز وضعية الدخول من الأسفل للكلمة الجديدة
              changingTxt.textContent = searchPhrases[currentIndex];
              changingTxt.classList.remove('slide-out');
              changingTxt.classList.add('slide-in');
              
              // 3. السماح للكلمة الجديدة بالاستقرار في موضعها الطبيعي
              setTimeout(() => {
                  changingTxt.classList.remove('slide-in');
              }, 40);

              currentIndex = (currentIndex + 1) % searchPhrases.length;
          }, 350);
      }

      // تبديل تلقائي مريح كل 3.5 ثوانٍ
      setInterval(triggerVerticalSlide, 3500);
      triggerVerticalSlide();

      // إدارة التفاعل: إخفاء الطبقة بمجرد الضغط أو الكتابة داخل البحث لمنع التداخل العشوائي
      boxWrapper.addEventListener('pointerdown', () => {
          overlay.style.opacity = '0';
      });
      
      // التفتيش عن الـ Input الداخلي بمكون سلة وتأمين حالته عند مسح النص
      setTimeout(() => {
          const sallaInput = boxWrapper.querySelector('input');
          if (sallaInput) {
              sallaInput.addEventListener('input', function() {
                  if (this.value.length > 0) {
                      overlay.style.display = 'none';
                  } else {
                      overlay.style.display = 'flex';
                      overlay.style.opacity = '1';
                  }
              });
              sallaInput.addEventListener('blur', function() {
                  if (this.value.length === 0) {
                      overlay.style.opacity = '1';
                  }
              });
          }
      }, 1000); // مهلة أمان لضمان رندرة مكوّن سلة بالكامل
  });
</script>