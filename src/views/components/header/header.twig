{#
| Variable         | Type    | Description                     |
|------------------|---------|---------------------------------|
| cart             | object  |                                 |
| cart.items_count | int     |                                 |
| cart.total       | string  | Formatted total ex: "١٠٠ ر.س"   |
#}

{% set important_links = theme.settings.get('important_links') %}
{% set logo_position = theme.settings.get('logo_position', 'center') %}

<header class="store-header">
  {# Top Nav #}
  <div class="top-navbar">
    <div class="container flex justify-between">
      <div class="flex-1 flex items-center gap-2">
          {% if important_links %}
            <salla-menu source="footer" topnav></salla-menu>
          {% endif %}

          <div class="header-buttons">
              {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
                  <salla-localization-modal show-trigger></salla-localization-modal>
              {% endif %}
          </div>

          {% if store.scope %}
              <button class="btn--rounded-gray"
                      onclick="salla.event.dispatch('scopes::open', {'mode': 'default'})">
                  <i class="sicon-location rtl:ml-1 ltr:mr-1"></i> {{ store.scope.name }}
              </button>
          {% endif %}

          <div class="header-search flex-1">
            <salla-search inline oval height="36"></salla-search>
          </div>
      </div>

      <salla-contacts is-header></salla-contacts>
    </div>
  </div>

  {# Main Nav #}
  <div id="mainnav" class="main-nav-container shadow-default bg-white flex items-center py-4">
      <div class="inner bg-inherit w-full">
          <div class="container">
              
              {# تقسيم الهيدر لـ 3 حارات مستقلة تماماً بنظام Grid #}
              <div class="grid grid-cols-3 w-full items-center relative" id="header-grid-layout">

                  {# 1. قسم اللوجو - مستقل ويتحرك بناءً على خيار لوحة التحكم #}
                  <div class="logo-block flex items-center {% if logo_position == 'center' %} justify-center col-start-2 col-span-1 {% else %} justify-start col-start-1 {% endif %}" id="logo-wrapper">
                      <a class="lg:hidden mburger mburger--collapse leading-none rtl:ml-4 ltr:mr-4" href="#mobile-menu" aria-label="Open-menu">
                        <i class="sicon-menu text-primary text-2xl"></i>
                      </a>

                      <a class="navbar-brand m-0" href="{{ store.url }}" style="max-width: 175px !important; display: block;">
                          <img fetchpriority="high" 
                               width="100%" 
                               height="100%" 
                               loading="eager" 
                               src="{{ store.logo|cdn(175) }}" 
                               alt="{{ store.name }} logo">
                          
                          {% if is_page('index') %}
                            <h1 class="sr-only">{{ store.name }}</h1>
                          {% else %}
                            <h2 class="sr-only">{{ store.name }}</h2>
                          {% endif %}
                      </a>
                  </div>

                  {# 2. قسم المنيو الرئيسي - معزول تماماً وثابت في مكانه (الحارة الوسطى افتراضياً) #}
                  <div class="menu-block flex {% if logo_position == 'center' %} col-start-1 col-span-3 row-start-2 justify-center mt-2 {% else %} col-start-2 justify-center {% endif %}" id="menu-wrapper">
                      <custom-main-menu></custom-main-menu>
                  </div>

                  {# 3. قسم السلة والحساب وتجربة الكتابة - ثابت دائماً يساراً #}
                  <div class="actions-block flex items-center justify-end col-start-3" id="actions-wrapper">
                      <div class="welcome-message text-green-600 font-bold mx-2 px-3 py-1 bg-green-50 rounded-full">
                         تجربة الكتابة
                      </div>
                      <salla-user-menu avatar-only show-header show-trigger></salla-user-menu>
                      <salla-cart-summary class="ml-4 rtl:ml-[unset] rtl:mr-4">
                        <i slot="icon" class="header-btn__icon icon sicon-shopping-bag"></i>
                      </salla-cart-summary>
                  </div>

              </div>

          </div>
      </div>
  </div>
</header>

{# 🎯 استهدفنا كلاس الـ body لسنترة اللوجو بشكل مستقل وعزل المنيو #}
{% if logo_position == 'center' %}
<style>
  body.body-logo-center .navbar-brand,
  body .navbar-brand {
      float: none !important;
      margin: 0 !important;
  }
  
  @media (min-width: 1024px) {
      #header-grid-layout {
          grid-template-columns: 1fr auto 1fr !important;
      }
      #logo-wrapper {
          grid-column: 2 / span 1 !important;
          grid-row: 1 !important;
      }
      #menu-wrapper {
          grid-column: 1 / span 3 !important;
          grid-row: 2 !important;
          margin-top: 12px !important;
      }
      #actions-wrapper {
          grid-column: 3 / span 1 !important;
          grid-row: 1 !important;
      }
  }
</style>
{% endif %}